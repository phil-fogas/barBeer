     <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="nav-link " aria-current="page" href="{{ path('home') }}"><i class="bi bi-house-fill"></i> Accueil</a>
 
  <i class="bi bi-tags-fill"></i> Categories:
  {% for category in categories %}
            
				<a class="nav-link {% if catid == category.id %}active{% endif %}" href="{{ path('showBeerCategory', { id : category.id}) }}">{{category.name}}</a>
			
   {% endfor %}

     {% if app.user %}
    <a class="nav-link" href="{{ path('app_logout') }}"><i class="bi bi-unlock-fill"></i> Deconnection</a>
     <a class="nav-link" href="{{ path('user') }}"><i class="bi bi-person-badge-fill"></i> Profil</a>   
    {% else %}
    <a class="nav-link" href="{{ path('app_login') }}"><i class="bi bi-lock-fill"></i> Connection</a>
      {% endif %} 
    </nav>